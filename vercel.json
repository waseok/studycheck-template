{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "buildCommand": "npm run vercel-build",
  "outputDirectory": "frontend/dist",
  "installCommand": "npm install && cd backend && npm install --include=dev && cd ../frontend && npm install --include=dev",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/onboarding/(.*)",
      "destination": "/api/onboarding-router?path=$1"
    },
    {
      "source": "/api/onboarding",
      "destination": "/api/onboarding-router"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ],
  "functions": {
    "api/onboarding-router.ts": {
      "maxDuration": 60,
      "includeFiles": "{backend/prisma/**,scripts/vercel-build.mjs}"
    },
    "api/ping.ts": {
      "maxDuration": 10
    },
    "api/health.ts": {
      "maxDuration": 10
    },
    "api/settings/status.ts": {
      "maxDuration": 15
    },
    "api/settings/public.ts": {
      "maxDuration": 15
    }
  }
}
